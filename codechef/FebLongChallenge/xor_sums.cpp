#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[n];
    }

    int q;
    cin >> q;

    int m[q];
    for (int i = 0; i < q; i++)
    {
        cin >> m[i];
    }

    for (int i = 0; i < q; i++)
    {
    }
}